package com.kotlinsun.aideskhub.voice

class VoiceInteractionController {
    fun startListening() {
        // Android STT and wake-word detection will be connected in a later issue.
    }

    fun speak(answer: String) {
        // Android TTS will be connected in a later issue.
    }
}
